# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Atlas DM-only build output (contains spoilers; never commit)
.local-atlas/

# Atlas backups (zip snapshots from `npm run atlas:backup`)
backups/

# Editor save-endpoint per-file backups (Phase 1A unified Save). Written by
# scripts/vite-plugin-atlas-save.ts before any overwrite, retained 3 deep
# per file path. Never committed.
.atlas-backups/

# Claude Code worktrees (live inside the main checkout but each has its own
# .git pointer; never stage them from the main repo).
.claude/worktrees/


# Visual-companion brainstorm artifacts
.superpowers/

# Editor diff baseline — local snapshot of the last deployed player atlas
# (D14). Written by publish-push after a successful push; never committed.
public/atlas/.last-published.json
